# Правила для каждого нового проекта

## ⛔ ГЛАВНОЕ ПРАВИЛО — НИКАКИХ ИЗМЕНЕНИЙ БЕЗ СОГЛАСОВАНИЯ

**Claude вносит изменения в код ТОЛЬКО если:**
1. Пользователь явно описал задачу
2. Claude объяснил что именно будет менять
3. Пользователь подтвердил («делай», «да», «ок» или аналог)

**Запрещено:**
- Менять логику, которую не обсуждали
- «Заодно» исправлять что-то ещё при выполнении задачи
- Откатывать или переписывать ранее сделанное без явной просьбы
- Добавлять фичи, рефакторинг, «улучшения» по своей инициативе

**Заметил баг или улучшение — сообщи и жди разрешения. Не трогай.**

---

## Рабочий процесс (в каждом проекте)

1. Пользователь ставит задачу
2. Claude объясняет план (коротко: что именно меняет и где)
3. Пользователь говорит «делай»
4. Claude делает — строго то, что обговорили
5. Коммит + пуш

---

## CLAUDE.md в каждом проекте

В корне каждого проекта создавать файл `CLAUDE.md` со следующими разделами:

```md
# Контекст проекта для Claude

## ⛔ ГЛАВНОЕ ПРАВИЛО
Никаких изменений без явного согласования. Заметил баг — сообщи и жди разрешения.

## Инфраструктура
- Фронтенд: Vercel (автодеплой из GitHub, ветка main)
- Бэкенд: Supabase (Edge Functions, Auth, DB)
- Репо: github.com/<user>/<repo>

## Стек
- React + Vite + TypeScript + Tailwind + shadcn/ui
- Framer Motion (анимации)
- Supabase JS клиент

## Среда Claude
- node_modules: нет (npm ci для установки)
- Supabase CLI: не работает
- Deno: не установлен локально

## Рабочий процесс
1. Claude пишет код → пушит в main
2. Vercel деплоит фронтенд (1-2 мин)
3. GitHub Actions деплоит Edge Functions (1-2 мин)
4. Тестируем на проде

## Открытые баги
_(список актуальных багов — обновлять в конце каждого чата)_
```

> **Правило**: В конце каждого чата — обновлять раздел «Открытые баги». Убирать то что пофикшено, добавлять новые. Костяк файла не трогать.

---

## Инфраструктура (стандартная настройка)

### Хостинг фронтенда — Vercel
- Подключить GitHub репо → автодеплой при пуше в `main`
- Переменные среды добавить в настройках Vercel

### Бэкенд — Supabase
- Создать проект на supabase.com
- Auth: включить Email OTP (не magic link!)
- Edge Functions: Deno, деплой через GitHub Actions

### GitHub Actions — автодеплой Edge Functions
Файл `.github/workflows/deploy.yml`:
```yaml
name: Deploy Supabase Functions
on:
  push:
    branches: [main]
    paths: ['supabase/functions/**']
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: supabase/setup-cli@v1
      - run: supabase functions deploy --project-ref ${{ secrets.SUPABASE_PROJECT_REF }}
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SBP_ACCESS_TOKEN }}
```

Секреты в GitHub: `SBP_ACCESS_TOKEN`, `SUPABASE_PROJECT_REF`

### GitHub Actions — автомерж веток
Файл `.github/workflows/automerge.yml`:
```yaml
name: Auto-merge to main
on:
  push:
    branches-ignore: [main]
jobs:
  merge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      - run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git checkout main
          git merge origin/${{ github.ref_name }} --no-ff -m "Auto-merge ${{ github.ref_name }}"
          git push origin main
```

---

## PWA (Progressive Web App)

### Иконки
- Генерировать через `@resvg/resvg-js` (devDependency)
- Файлы: `public/icon-192.png`, `public/icon-512.png`, `public/favicon.ico`, `public/favicon.svg`
- ICO формат создавать через Node.js скрипт (без ImageMagick)

### manifest.json
```json
{
  "name": "Название приложения",
  "short_name": "Краткое",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#0f0f11",
  "theme_color": "#0f0f11",
  "icons": [
    { "src": "/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "/icon-512.png", "sizes": "512x512", "type": "image/png" }
  ]
}
```

---

## Auth (Supabase OTP)

Всегда использовать **Email OTP** (не magic link):
- Шаг 1: `supabase.auth.signInWithOtp({ email })` → отправляет код
- Шаг 2: `supabase.auth.verifyOtp({ email, token, type: 'email' })` → проверяет код
- Код: 8 цифр (не 6!), приходит в письме

---

## Дизайн-система

Если в проекте есть папка `kino-design-system/` или аналог — **всегда смотреть туда** перед любыми UI-изменениями. Не выдумывать компоненты с нуля.

---

## Установленная среда (на машине Claude)

### Runtime
- Node.js v22
- npm v10
- Git v2.43

### Типовой стек
- React + Vite + TypeScript + Tailwind + shadcn/ui
- Framer Motion

### Пакеты (использовались в проектах)
- `xlsx` — Excel-парсинг
- `@resvg/resvg-js` — SVG → PNG (devDep)
- `zod` — валидация

### Чего нет
- Supabase CLI — не работает
- Deno — нет локально
- ImageMagick, rsvg-convert — нет
- node_modules — нет по умолчанию (установка: `npm ci`)

> **Правило**: При установке нового пакета или инструмента — добавить его в `~/.claude/CLAUDE.md` в раздел «Установленная среда».

---

## Стиль общения Claude

- Коротко и по делу
- Без лишних объяснений если не просят
- Без инициативных изменений
- Отвечать на языке пользователя
